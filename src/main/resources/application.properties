spring.application.name=recensement
# ==== Base de donnees (variables d'environnement, jamais de valeurs en dur) ====
spring.datasource.url=jdbc:postgresql://dpg-da0m1g61egvs7392aa2g-a.oregon-postgres.render.com/recensement_x2do
spring.datasource.username=recensement_x2do_user
spring.datasource.password=BwmdbrAH0U4QfIzYkvjYNull9g4t2rnd
spring.datasource.driver-class-name=org.postgresql.Driver

# ==== JPA / Hibernate ====
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==== Serveur ====
server.port=${PORT:8080}

# ==== Swagger / OpenAPI ====
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs

# ==== CORS (utilise dans le WebConfig) ====
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}
